# Fail fast in CI rather than silently resolving a different tree.
engine-strict=true

# Keep the dependency graph honest: a package must declare what it imports.
auto-install-peers=false
strict-peer-dependencies=false

# Workspace packages always resolve to the local source, never the registry.
link-workspace-packages=true
prefer-workspace-packages=true
save-workspace-protocol=rolling
